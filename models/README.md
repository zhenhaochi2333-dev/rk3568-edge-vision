# Local model

The formal EdgeVision target uses the YOLO11s INT8 RKNN model
`yolo11s_rk3568_i8.rknn`. The binary is intentionally ignored by Git until its
redistribution permission, origin, and license are audited.

Stage the verified model locally for development or board deployment; do not
place generated model artifacts in normal Git history.
